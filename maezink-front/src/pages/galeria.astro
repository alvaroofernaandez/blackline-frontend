---
import fs from 'fs';
import path from 'path';
import Layout from '../layouts/Layout.astro';
import GaleriaComponent from '../components/galeria/Galeria.jsx';

const galeriaDir = path.resolve('./public/galeria');
const files = fs.readdirSync(galeriaDir).filter(file =>
  /\.(jpg|jpeg|png|webp|avif)$/i.test(file)
);

---

<Layout title="Maez Ink - Galería">
  <img src="/favicon.svg" transition:name="vtImg" alt="logo" class="mt-28 mx-auto w-32 max-w-xs sm:max-w-sm md:max-w-md lg:max-w-lg" loading="lazy">
  <section class="px-4 py-12 text-white max-w-4xl mx-auto  ">
    <div class="columns-1 sm:columns-2 md:columns-3 lg:columns-5 gap-4 space-y-4">
      <GaleriaComponent images={files} client:load/>
    </div>
  </section>
</Layout>

